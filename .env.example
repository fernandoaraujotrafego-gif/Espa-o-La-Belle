# O aplicativo não exige chaves secretas no navegador.
# A configuração pública do Firebase está versionada em src/lib/firebase.ts.

# Opcional: desative o recarregamento automático durante edições por agentes.
DISABLE_HMR=false
